## 变更内容 / What changed

请简要说明本次改动的内容与原因。

<!-- 在此填写 -->

## 变更类型 / Type of change

- [ ] Bug fix
- [ ] New feature
- [ ] Dependency update
- [ ] Build / CI change
- [ ] Documentation
- [ ] Refactor / no behavior change

## 验证情况 / Verification

- [ ] `cd android && ./gradlew :app:compileDebugKotlin` passes (if `android/` changed)
- [ ] `cd enimul && go build ./...` passes (if Go changed)
- [ ] `cd enimul && go test ./internal/core/...` passes (if engine changed)
- [ ] `cd tun2socks && go build ./...` passes (if `tun2socks/` changed)
- [ ] Commits are conventional, written in English, and GPG-signed

## 关联 Issue / Related issues

仅当本 PR 确实解决某 issue 时使用 `Fixes #N` / `Closes #N`。

Fixes #

## 测试备注 / Notes for reviewers

设备与 Android 版本、网络环境、难以自动验证的行为、UI 截图等。

<!-- 在此填写 -->
